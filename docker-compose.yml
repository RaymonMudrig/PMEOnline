# Docker Compose for LOCAL DEVELOPMENT ONLY
# For production deployment, use individual docker run commands
# See DEPLOYMENT.md for production deployment instructions

version: '3.9'

services:
  # ==================== Infrastructure (Local Dev) ====================

  kafka:
    image: apache/kafka:3.8.1
    container_name: pme-kafka
    ports:
      - "9092:9092"
    environment:
      KAFKA_NODE_ID: 1
      KAFKA_PROCESS_ROLES: broker,controller
      KAFKA_LISTENERS: PLAINTEXT://0.0.0.0:9092,CONTROLLER://0.0.0.0:9093
      KAFKA_ADVERTISED_LISTENERS: PLAINTEXT://localhost:9092
      KAFKA_CONTROLLER_LISTENER_NAMES: CONTROLLER
      KAFKA_LISTENER_SECURITY_PROTOCOL_MAP: CONTROLLER:PLAINTEXT,PLAINTEXT:PLAINTEXT
      KAFKA_CONTROLLER_QUORUM_VOTERS: 1@localhost:9093
      KAFKA_OFFSETS_TOPIC_REPLICATION_FACTOR: 1
      KAFKA_TRANSACTION_STATE_LOG_REPLICATION_FACTOR: 1
      KAFKA_TRANSACTION_STATE_LOG_MIN_ISR: 1
      KAFKA_GROUP_INITIAL_REBALANCE_DELAY_MS: 0
      KAFKA_NUM_PARTITIONS: 3
    volumes:
      - kafka-data:/var/lib/kafka/data
    healthcheck:
      test: ["CMD", "kafka-broker-api-versions.sh", "--bootstrap-server", "localhost:9092"]
      interval: 10s
      timeout: 5s
      retries: 5
    networks:
      - pme-network

  postgres:
    image: postgres:16-alpine
    container_name: pme-postgres
    ports:
      - "5432:5432"
    environment:
      POSTGRES_USER: pmeuser
      POSTGRES_PASSWORD: pmepass
      POSTGRES_DB: pmedb
    volumes:
      - postgres-data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U pmeuser -d pmedb"]
      interval: 5s
      timeout: 5s
      retries: 5
    networks:
      - pme-network

  # ==================== PME Services ====================
  # Uncomment to run services in Docker (optional for local dev)
  # By default, run services with 'go run' for faster development

  # dbexporter:
  #   build:
  #     context: .
  #     dockerfile: cmd/dbexporter/Dockerfile
  #   container_name: pme-dbexporter
  #   environment:
  #     KAFKA_URL: kafka:9092
  #     KAFKA_TOPIC: pme-ledger
  #     DB_HOST: postgres
  #     DB_PORT: 5432
  #     DB_USER: pmeuser
  #     DB_PASSWORD: pmepass
  #     DB_NAME: pmedb
  #   depends_on:
  #     kafka:
  #       condition: service_healthy
  #     postgres:
  #       condition: service_healthy
  #   networks:
  #     - pme-network

  # eclearapi:
  #   build:
  #     context: .
  #     dockerfile: cmd/eclearapi/Dockerfile
  #   container_name: pme-eclearapi
  #   ports:
  #     - "8081:8081"
  #   environment:
  #     KAFKA_URL: kafka:9092
  #     KAFKA_TOPIC: pme-ledger
  #   depends_on:
  #     kafka:
  #       condition: service_healthy
  #   networks:
  #     - pme-network

  # pmeoms:
  #   build:
  #     context: .
  #     dockerfile: cmd/pmeoms/Dockerfile
  #   container_name: pme-oms
  #   environment:
  #     KAFKA_URL: kafka:9092
  #     KAFKA_TOPIC: pme-ledger
  #   depends_on:
  #     kafka:
  #       condition: service_healthy
  #   networks:
  #     - pme-network

  # pmeapi:
  #   build:
  #     context: .
  #     dockerfile: cmd/pmeapi/Dockerfile
  #   container_name: pme-api
  #   ports:
  #     - "8080:8080"
  #   environment:
  #     KAFKA_URL: kafka:9092
  #     KAFKA_TOPIC: pme-ledger
  #   depends_on:
  #     kafka:
  #       condition: service_healthy
  #   networks:
  #     - pme-network

networks:
  pme-network:
    driver: bridge

volumes:
  kafka-data:
  postgres-data:
